<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow
                          http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">
	
	<!-- If not specified, the start state is the first state specified. -->
	<view-state id="AgeEnter" view="AgeEnter">
		<transition on="submit" to="AgeSave" />
	</view-state>

	<action-state id="AgeSave">
		<evaluate expression="struts2FlowAdapter.execute(flowRequestContext)" />
		<transition on="input" to="AgeEnter" />
		<transition on="success" to="CalcRate" />
	</action-state>
	
	<action-state id="CalcRate">
		<evaluate expression="struts2FlowAdapter.execute(flowRequestContext)" />
		<transition on="success" to="diplayRate" />
	</action-state>

	<view-state id="diplayRate" view="diplayRate">
		<transition on="finish" to="finish" />
		<transition on="startOver" to="AgeEnter" />
	</view-state>

	<end-state id="finish" view="finish"/>
</flow>